function [output] = matlab_dialog(target_positions, target)
plot(target_positions(target, :))
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

%msgbox('a')

%close all

%[target_count,position_count] = size(target_positions);

%msgbox('b')

%target = 1;

%while 1
    %msgbox('c')
    %target_position = target_positions(mod(target, target_count) + 1, :);
    %msgbox('d')
    %plot(target_position)
    %msgbox('e')
    %target = target + 1;
    %msgbox('f')
    %pause(1)
%end

end

